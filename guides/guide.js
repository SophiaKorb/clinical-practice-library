(()=>{const key="barrier-guide:"+(document.body.dataset.guide||location.pathname);const controls=[...document.querySelectorAll("[data-save]")];let saved={};try{saved=JSON.parse(localStorage.getItem(key)||"{}")}catch{}controls.forEach((el,i)=>{const k=el.id||"field-"+i;if(el.type==="checkbox")el.checked=!!saved[k];else if(saved[k]!=null)el.value=saved[k];el.addEventListener("input",()=>{saved[k]=el.type==="checkbox"?el.checked:el.value;localStorage.setItem(key,JSON.stringify(saved))})});const clear=document.getElementById("clearGuide");if(clear)clear.addEventListener("click",()=>{if(!confirm("Clear saved selections and notes for this guide?"))return;localStorage.removeItem(key);controls.forEach(el=>{if(el.type==="checkbox")el.checked=false;else el.value=""})})})();